/// Core logic for Atelier: project model, session management, conversation engine.
///
/// This target contains no SwiftUI imports — all UI lives in `Atelier` (app) and
/// `AtelierDesign` (design system). Business logic stays portable.
public enum AtelierKit {
    public static let version = "0.1.0"
}
